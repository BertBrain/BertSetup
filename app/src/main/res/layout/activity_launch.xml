<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="bert.ui.launchScreen.LaunchActivity"
    android:background="@color/LightGreen">
    <ImageButton
        android:scaleX="0.75"
        android:scaleY="0.75"
        android:background="#FFFFFF"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/startScreenImage"
        android:src="@drawable/startup_splash"
        android:focusable="false"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Audit"
        android:id="@+id/startAuditButton"
        android:layout_alignBottom="@+id/startScreenImage"
        android:layout_alignLeft="@+id/startScreenImage"
        android:layout_alignStart="@+id/startScreenImage" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Install"
        android:id="@+id/startInstallButton"
        android:layout_alignBottom="@+id/startScreenImage"
        android:layout_alignRight="@+id/startScreenImage"
        android:layout_alignEnd="@+id/startScreenImage"
        android:layout_alignParentEnd="false" />
</RelativeLayout>
